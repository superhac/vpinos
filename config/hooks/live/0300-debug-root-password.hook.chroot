#!/bin/sh
set -e

# Development-only: gives console access via the normal getty on tty2+
# (tty1 autologs in as `vpinos`) for debugging boot/weston issues.
# Remove before shipping a real image.
echo "root:vpinos" | chpasswd
